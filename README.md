# vscode-config
My personal VS Code config, for C++/trading-adjacent dev work.

## Setup

```bash
git clone https://github.com/<you>/vscode-config.git
cd vscode-config
./install.sh
```

AI/Copilot chat features are disabled in `settings.json` by preference.