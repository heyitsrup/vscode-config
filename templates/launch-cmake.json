{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/<executable-name>",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake Build"
        }
    ]
}
